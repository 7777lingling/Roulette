name: Admin Permission Test

on:
  workflow_dispatch:

jobs:
  test-admin:
    runs-on: [self-hosted]
    steps:
      - name: Show current user
        run: whoami /groups

      - name: Try to run elevated command
        run: |
          echo Hello from admin runner!
